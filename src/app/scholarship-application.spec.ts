import { ScholarshipApplication } from './models/scholarship-application';

describe('ScholarshipApplication', () => {
  it('should create an instance', () => {
    expect(new ScholarshipApplication()).toBeTruthy();
  });
});
